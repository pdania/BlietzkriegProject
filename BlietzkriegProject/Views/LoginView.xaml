<Page
    x:Class="BlietzkriegProject.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlietzkriegProject.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Height="350" Width="500">

    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Input card number and password" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBox x:Name="Name" Header= "Card number" Margin="0,24,0,0" MaxLength="16" Width="400" HorizontalAlignment="Left" InputScope="PersonalFullName"/>
            <PasswordBox x:Name="PhoneNumber" Header="PIN" Margin="0,24,0,0" MaxLength="4" Width="400" HorizontalAlignment="Left" InputScope="TelephoneNumber" />
            <StackPanel Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Stretch">
                <Button Content="Enter" Margin="0,24"/>
                <Button Content="Cancel" Margin="24"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
